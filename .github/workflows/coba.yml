name: Test repository dispatch

on:
  repository_dispatch:
    types: [on-demand-test]

jobs:
  analyze:
    runs-on: ubuntu-latest

    steps:
      - name: Echo
        run: |
          echo "Yeay bisa akhirnya"
